<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog.ejs</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body>
    <div class="text-center my-8">
        <h2 class="text-3xl font-semibold">Popular Blogs</h2>
    </div>

    <div class="flex flex-wrap justify-center gap-6 px-4 my-8">
        <% if (blogs.length > 0) { %>
        <% blogs.forEach(blog => { %>
        <a href="/blogs/<%= blog._id %>" class="block p-6 bg-white rounded-lg shadow-md hover:bg-gray-50 w-full md:w-1/3 lg:w-1/4">
            <h2 class="text-xl font-semibold mb-2"><%= blog.title %></h2>
            <p class="text-gray-700 text-sm">
                <%= blog.body.substring(0, 40) %>...
            </p>
        </a>
        <% }) %>
        <% } else { %>
        <h2 class="text-xl font-bold text-center w-full">
            No Blogs Found!
        </h2>
        <% } %>
    </div>
</body>

</html>
